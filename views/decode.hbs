<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Decode tokens</title>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
                <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
                    <span class="fs-4 btn btn-outline-primary me-2">Back to Discovery</span>
                </a>

                <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="/api/protocol/logger" class="nav-link">Protocol</a></li>
                </ul>

                <a class="col-md-3 text-end">
                    <span class="fs-4 btn btn-primary me-2">To Authentication</span>
                </a>
            </header>
        </div>
        <div class="container">
            <form class="input-form" enctype="multipart/form-data" action="/api/token/decode" method="post">
                <div class="card card-body d-flex flex-row align-content-start w-100">
                    <label class="w-25" for="issuer">Issuer:</label>
                    <input class="w-75" type="text" id="issuer" name="issuer" />
                </div>
                <div class="card card-body d-flex flex-row align-content-start w-100">
                    <label class="w-25" for="token">Access-Token:</label>
                    <textarea class="w-75" type="text" id="token" name="token" rows="8"></textarea>
                </div><br>
                <p>
                <a class="btn btn-primary" data-bs-toggle="collapse" href="#advancedSettings" role="button" aria-expanded="false" aria-controls="advancedSettings">
                    Advanced Settings
                </a>
                </p>
                <div class="collapse flex-wrap align-items-center w-100" id="advancedSettings">
                    <div class="card card-body d-flex flex-row align-content-start w-100">
                        <label class="w-50" for="schema_selector"> Select schema (uploaded or local): </label>
                        <div class="d-flex flex-column align-content-start w-50">
                            <div class="d-flex flex-row align-content-start">
                                <label class="w-50" for="schema_uploaded_selector"> Uploaded: </label>
                                <select class="w-50" name="schema" id="schema_uploaded_selector">
                                    {{#if schemas}}
                                    {{#each schemas as |schema|}}
                                    <option value="{{ schema }}">{{ schema }}</option>
                                    {{/each}}
                                    {{/if}}
                                </select>
                            </div><br>
                            <div class="d-flex flex-row align-content-start">
                                <label class="w-50" for="schema_local_selector"> Local: </label>
                                <input class="w-50" type="file" id="schema_local_selector" name="schema_file">
                            </div>
                        </div>
                    </div>
                    <div class="card card-body d-flex flex-row align-content-start w-100">
                        <label class="w-50" for="getKeysFromProvider">Use Key-Material from JWKS-Endpoint?</label>
                        <input class="w-50" type="checkbox" id="getKeysFromProvider" name="getKeysFromProvider" checked="checked"/>
                    </div>
                    <div class="card card-body d-flex flex-column align-content-start w-100">
                        <div class="d-flex flex-row align-content-start w-100">
                            <label class="w-50" id="keyMaterialAlgorithmLabel" for="keyMaterialAlgorithm">Key-Algorithm:</label>
                            <select class="w-50" id="keyMaterialAlgorithm" name="keyMaterialAlgorithm">
                                {{#if key_algorithms}}
                                {{#each key_algorithms as |key_alg|}}
                                <option id="{{key_alg}}" value="{{key_alg}}">{{key_alg}}</option>
                                {{/each}}
                                {{/if}}
                            </select>
                        </div><br>
                        <div class="d-flex flex-row align-content-start w-100">
                            <label class="w-50" id="keyMaterialFilepathLabel" for="keyMaterialFilepath">Key-Filepath:</label>
                            <input class="w-50" type="text" id="keyMaterialFilepath" name="keyMaterialFilepath" />
                        </div>
                    </div><br>
                    <div class="card card-body d-flex flex-row align-content-start w-100">
                        <label class="w-50" for="file_upload">Upload schema: </label>
                        <input form="upload_form" class="w-25 mx-4" type="file" id="file_upload" name="upload">
                        <input form="upload_form" class="btn btn-lg btn-primary w-25 mx-0 mb-2" type="submit" value="Upload schema">
                    </div>
                    {{#if schemas}}
                    <div class="card card-body d-flex flex-row align-content-start w-100">
                        <label class="w-50" for="schema_download_selector"> Select schema: </label>
                        <select form="download_form" class="w-25 mx-4" name="schema" id="schema_download_selector">
                            {{#each schemas as |schema|}}
                            <option value="{{ schema }}">{{ schema }}</option>
                            {{/each}}
                        </select>
                        <input form="download_form" class="btn btn-lg btn-primary w-25 mx-0 mb-2" type="submit" value="Download schema">
                    </div>
                    <div class="card card-body d-flex flex-row align-content-start w-100">
                        <label class="w-50" for="schema_delete_selector"> Select schema: </label>
                        <select form="delete_form" class="w-25 mx-4" name="schema" id="schema_delete_selector">
                            {{#each schemas as |schema|}}
                            <option value="{{ schema }}">{{ schema }}</option>
                            {{/each}}
                        </select>
                        <input form="delete_form" class="btn btn-lg btn-primary w-25 mx-0 mb-2" type="submit" value="Delete schema">
                    </div><br>
                    {{/if}}
                </div>
                <div class="button-group">
                    <button class="btn btn-outline-primary" type="submit" formaction="/api/token/decode" formmethod="get">Reset</button>
                    <button class="btn btn-primary" type="submit">Submit</button>
                </div>
            </form>                
            <form id="upload_form" enctype="multipart/form-data" method="post" action="/api/token/schema/upload"></form>
            <form id="download_form" action="/api/token/schema/download"></form>
            <form id="delete_form" action="/api/token/schema/delete"></form>
        </div><br>
        <div class="container">
            <p>{{message}}</p>
            {{#if showResults}}
            {{#if schema_error}}
                <p>
                <a class="btn btn-danger" data-bs-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                    Toggle Error
                </a>
                </p>
            {{else}}
                <p>
                <a class="btn btn-primary" data-bs-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                    Toggle Results
                </a>
                </p>
            {{/if}}
            <div class="collapse show" id="collapseExample">
                <div class="card card-body">
                    <h2>Header</h2>
                    <pre>
{{header}}
                    </pre>
                </div>
                <div class="card card-body">
                    <h2>Payload</h2>
                    <pre>
{{{payload}}}
                    </pre>
                </div>
            </div>
            {{/if}}
        </div>
        <script
                src="https://code.jquery.com/jquery-3.6.0.js"
                integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk="
                crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    </body>
</html>
